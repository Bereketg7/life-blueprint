/** Shared analytics constants to avoid magic numbers across services. */

/** Default daily calorie target when user's target is not recorded. */
export const DEFAULT_TARGET_CALORIES = 2000;

/** Approximate kcal equivalent of 1 kg of body fat. */
export const KCAL_PER_KG_FAT = 7700;

/** Minimum recommended sleep hours per night. */
export const MIN_RECOMMENDED_SLEEP_HRS = 7;

/** Maximum recommended daily calorie deficit for healthy weight loss. */
export const MAX_SAFE_DAILY_DEFICIT = 500;
